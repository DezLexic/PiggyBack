# packages

Reserved for shared artifacts later (for example OpenAPI-generated TypeScript types, shared constants, or validation schemas). No shared packages are required for the initial scaffolding; add them when the API surface stabilizes.
